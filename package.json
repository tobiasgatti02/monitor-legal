{
  "name": "monitor-legal",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace @monitor-legal/web run dev",
    "build": "npm --workspace @monitor-legal/web run build",
    "lint:web": "npm --workspace @monitor-legal/web run lint",
    "test:web": "npm --workspace @monitor-legal/web run test",
    "typecheck": "npm --workspace @monitor-legal/web run typecheck"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
